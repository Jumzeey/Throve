# Throve Admin Console

Mock Premium Editorial admin UI (Vite + React + Tailwind + shadcn).

```bash
# from repo root
npm run start:admin
# or
npm run dev --workspace admin
```

Runs on http://localhost:5180
